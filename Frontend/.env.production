# Production — set this to your deployed backend URL (no trailing slash)
# This value is overridden at build time by GitHub Actions using the VITE_API_URL secret.
# For testing: set secret to http://YOUR_VPS_IP
# For production: set secret to https://viwaha.com
VITE_API_URL=https://api.viwaha.com
